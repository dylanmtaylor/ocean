# Scalr Integration

An Ocean integration for Scalr that syncs environments, workspaces, runs, and modules into Port.

## Configuration

- `scalrBaseUrl` - The hostname of your Scalr account (e.g. `example.scalr.io`)
- `scalrToken` - Scalr API token
